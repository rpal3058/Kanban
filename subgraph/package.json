{
  "name": "kanban",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ kanban",
    "create-local": "graph create --node http://localhost:8020/ kanban",
    "remove-local": "graph remove --node http://localhost:8020/ kanban",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kanban"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
